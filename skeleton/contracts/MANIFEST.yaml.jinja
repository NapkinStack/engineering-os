module:
  name: contracts
  responsibility: >
    Définit et versionne les contrats d'interface entre modules.
  owner: {{ owner_team }}
  contact: "https://github.com/{{ github_repo }}/discussions"
  lifecycle: Actif
  criticality: eleve

provides: []
consumes: []
data:
  owns: []
  shared: []
dependencies: []

commands:
  check: "echo 'commands.check à déclarer : validation des schémas de contrat' >&2; exit 1"
  test: "echo 'commands.test à déclarer : contract tests' >&2; exit 1"

docs:
  readme: README.md
  agents: AGENTS.md
  runbook: docs/runbook.md
